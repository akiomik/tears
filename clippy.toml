# Clippy configuration for tears

# Valid identifiers in documentation that should not require backticks
# The ".." appends to the default list instead of replacing it
doc-valid-idents = ["TanStack", ".."]
